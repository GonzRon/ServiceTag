# B07 — the search box leaves the Dashboard and lives on the Assets screen (owner-directed, 2026-09-23)

**Owner instruction (2026-09-23):** "remove the search assets and components search bar from the dashboard, for a cleaner look now that category and maintenance status drop downs appear as navigation aids, and instead move that filter / search bar to the assets [screen]." Rides in 1.3.0 as an owner-directed UI change; not part of #43's contract. Runs in Wave 2 beside B02's review (files disjoint: B02 is `:core`; this brief is `ui/dashboard` and `ui/asset` and their tests) and **must merge before Wave 3 opens** (B04 later edits `ui/asset/`). This lane holds `emulator-5554` for its connected tests.

**Global constraints:** master plan §1 (commit rules; no private data or `/home` paths; gitlink `7e0377a`; `$BASE` = the lane's base commit; every user-visible string from the ratified ledger — **this brief adds NO string**: every sentence it shows already ships).

**Files**
- Modify `app/src/main/kotlin/com/loosecannon/servicetag/ui/dashboard/DashboardScreen.kt` — remove the `SearchBox` composable (the block documented as "The quick filter (#39)", ~:228–:266) and its call site (~:133) and every branch that depended on a non-blank query (the "search found nothing" copy path). The category and maintenance-status dropdowns (`DashboardFilters.kt`) are untouched.
- Modify `app/src/main/kotlin/com/loosecannon/servicetag/ui/dashboard/DashboardViewModel.kt` — remove `query` (state and `StateFlow`), `onQueryChange`, `clearQuery`, `admittedBy(query, …)` on due rows, and the query-dependent branches in `build(...)`: the list is the in-service **systems** exactly as a blank query showed them (components stay hidden), the due rows are no longer narrowed by a query, the category/status filters keep working as today. Move `SEARCHED_FIELDS` and `Asset.matches(query)` out of this file into the new `AssetSearch.kt` below.
- Create `app/src/main/kotlin/com/loosecannon/servicetag/ui/asset/AssetSearch.kt` — `SEARCHED_FIELDS` and `Asset.matches(query)` (unchanged semantics: case-insensitive substring over the same six fields; blank matches everything) and the `SearchBox` composable moved here **verbatim** (placeholder and `contentDescription` "Search assets and components", trailing "Clear search" only when non-empty, `OutlinedTextField`, `ControlShape`).
- Modify `app/src/main/kotlin/com/loosecannon/servicetag/ui/asset/AssetsScreen.kt` — the `SearchBox` sits directly under the top bar, above the "Show archived" chip, full width; when the query is non-blank and nothing matches, show the same sentence the Dashboard showed for that case (take it verbatim from the removed Dashboard branch; if the Dashboard had none, show nothing but the empty list — do not invent copy). The existing empty states ("No active assets · N archived", the Add asset button) are unchanged for a blank query.
- Modify `app/src/main/kotlin/com/loosecannon/servicetag/ui/asset/AssetViewModels.kt` — `AssetsViewModel` gains `query: StateFlow<String>`, `onQueryChange(value)`, `clearQuery()`; `AssetsState` gains `query`; filtering composes with "Show archived" exactly as today: blank → the list as today; non-blank → rows whose asset `matches(query)`, **including components**, each component row naming its system the way the Dashboard's search result did (reuse whatever row/label the Dashboard used for "a component names its system"; if the Assets row type cannot carry a parent name, show the component under its system's name in the existing row shape — no new string).
- Tests — Modify `app/src/test/.../ui/dashboard/DashboardViewModelTest.kt` and `DashboardViewModelMaintenanceTest.kt` (drop the query cases; keep everything else; the file must still compile and every remaining case still asserts something); Modify `app/src/test/.../ui/asset/AssetViewModelsTest.kt` (add: blank query lists as before; a name match; a category match; a component appears only when searched and names its system; clear restores; the "Show archived" chip still composes with a query); Delete `app/src/androidTest/.../ui/dashboard/DashboardSearchTest.kt` and Create `app/src/androidTest/.../ui/asset/AssetsSearchTest.kt` carrying its two scenarios re-targeted at the Assets screen (`aComponentIsHiddenUntilItIsSearchedForAndThenNamesItsSystem`, `anEmptyListUnderAnEmptyBoxIsNotToldItsSearchFoundNothing`) with `clearInstall()` in `@Before`; add to an existing dashboard connected class (e.g. `DashboardAttentionTest`) one case asserting `onNodeWithContentDescription("Search assets and components").assertDoesNotExist()` on the Dashboard.

**Hazards (one test each):** the Dashboard no longer renders the search field; the Dashboard list and due rows are unchanged for what a blank query used to show (a JVM test on the view model: same rows as before with no query API); the Assets screen filters by each of the six fields (at least name and category asserted), components appear only under a non-blank query and name their system, blank restores the full list, clear restores, the no-match sentence (if any) appears only under a non-blank query with an empty result, "Show archived" still composes.

**Gate:** `./gradlew :app:testDebugUnitTest --console=plain` green (whole suite); connected on `emulator-5554` with `ANDROID_SERIAL=emulator-5554`, one package per invocation: `-Pandroid.testInstrumentationRunnerArguments.package=com.loosecannon.servicetag.ui.dashboard`, `…ui.asset`, and `-Pandroid.testInstrumentationRunnerArguments.class=com.loosecannon.servicetag.ui.AppSmokeTest` — all green, 0 skipped; `git diff --stat $BASE..HEAD` touches only the files above; `grep -rn '"Search assets and components"' app/src/main` → exactly the moved sites (Assets), none under `ui/dashboard`; no new string literal in `app/src/main` under `ui/asset` or `ui/dashboard` that is not already in the tree at `$BASE`.

**Must not:** add or change any user-visible sentence; touch `ui/components/**`, `core/**`, `api/**`, `share/**`, the manifest, or any B02–B06 file; change the category/status dropdown behaviour.

**Report:** `.superpowers/sdd/2026-09-23-servicetag-share-intake/B07-report.md` (status, commits, per-hazard test names, unit + connected counts, deviations).
