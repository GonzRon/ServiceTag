# B02 — explicit zone seam, test tidies, version 1.2.1 (Lane B of 1.2.1)

Read `plan.md` in this folder first (owner ruling 2: keep D-27's device-zone floor exactly; global constraints; the Lane B contract). Anchors:

- `core/src/main/kotlin/com/loosecannon/servicetag/core/usecase/RecomputeSchedules.kt:53-62` — the `zone: () -> ZoneId = { ZoneId.systemDefault() }` default; make it required, keep the KDoc's reason for a provider (the zone can change under a long-lived process). `app/src/main/kotlin/com/loosecannon/servicetag/AppGraph.kt` constructs it (~line 187) — pass `{ ZoneId.systemDefault() }` explicitly, next to the existing `today`/`clock` arguments. Find every other constructor call with `git grep -n 'RecomputeSchedules('` (tests, `FakeGraph`, the restore proof) and keep each one's existing zone value.
- `app/src/androidTest/kotlin/com/loosecannon/servicetag/ui/maintenance/HealthScreenTest.kt` — add `@Before fun freshInstall() = clearInstall()` (the `internal fun clearInstall()` lives in `app/src/androidTest/kotlin/com/loosecannon/servicetag/ui/AppSmokeTest.kt`; same module, import from the `ui` package). Keep its `performScrollTo()`. You cannot run connected tests; the controller runs them at the gate.
- `app/src/androidTest/kotlin/com/loosecannon/servicetag/ui/dashboard/DashboardAttentionTest.kt:69` — the comment only.
- `America/New_York`: `app/src/test/.../reminders/DigestAlarmTest.kt:23`, `app/src/test/.../ui/maintenance/ScheduleDetailViewModelTest.kt:198`, `app/src/test/.../ui/maintenance/StatusVocabularyTest.kt:75`, `core/src/test/.../core/schedule/ScheduleRecomputeTest.kt:102,118,133`. Read each test to see whether it exercises a DST transition; use `Etc/GMT+5` (UTC−5, no DST) where it does not; otherwise a non-US DST zone and say so; the tests must still fail if the zone were UTC (that is their point).
- Version: `app/build.gradle.kts:45-50`; `docs/versioning.md:34` (the 1.2.0 row is the shape; add 1.2.1 | 14 | PATCH …); run `:app:testDebugUnitTest --tests '*VersionAgreementTest*'` — it reads the docs and tells you what else must agree (README, etc.).
- Gate: `./gradlew :nfc-core:test :nfc-android:testDebugUnitTest :core:test :app:testDebugUnitTest --rerun-tasks --console=plain` green; record the total in the versioning row. Report: `.superpowers/sdd/2026-09-23-servicetag-1.2.1/B02-report.md`.
