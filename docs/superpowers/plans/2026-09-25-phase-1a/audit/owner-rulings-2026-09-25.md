# Owner rulings on the Phase 1A audit (2026-09-25) — verbatim summary

Audit accepted. Proceed with Phase 1A reconciliation and planning, using the audit findings as the new source of truth. Do not implement from stale issue wording. First update the GitHub issues and #76 roadmap metadata; then prepare the implementation sequence; do not begin implementation until the reconciliation is committed/documented and the plan reported.

**#54 — ACCEPT REWRITE/NARROW.** Only the five floating external actions in `.github/workflows/ci.yml`'s `build` job. `release.yml`, `mcp`, `bundle`, `schedules` already pinned. Verify each SHA corresponds to the intended current action release; never infer a SHA from the major tag. Acceptance: five full SHA pins with trailing version comments and one green CI run on the exact commit.

**#65 — ACCEPT REWRITE/NARROW.** The previous diagnosis is superseded; no `MainDispatcherRule`. Title: "AttachmentsSectionViewModelTest: ViewModel IO work can outlive test lifetime and cause a full-suite flake". Preserve both historical sightings; record that per-test setMain/resetMain exists, ViewModelStore.clear() exists, isolated stress runs do not reproduce it, the likely mechanism is production Dispatchers.IO work continuing past the awaited signal and teardown. Direction: injectable IO context (production default Dispatchers.IO); tests use their StandardTestDispatcher(scheduler); cancellation/clear while runTest can still drain; replace the blocking progress-test latch with a suspending gate if necessary; close the `_saved` two-concurrent-emits race if confirmed. BOUNDED attempt to capture the full failure cause chain from full-suite runs first; if no reproduction after a reasonable bounded set, record it and do the deterministic lifetime fix. Proof = repeated FULL-SUITE runs; isolated repetitions secondary. May inspect ReferencesSectionViewModel, BackupViewModel and Scan for the same pattern; no broad refactor unless directly equivalent and low-risk; wider findings become follow-ups.

**#66 — ACCEPT REWRITE/NARROW.** Not an Android-17 platform change. Title: "Developer API: explain revocable network-permission denial and provide recovery". Record: stock Android 17 keeps INTERNET normal/install-time; the owner's hardened Android build exposes it as revocable; `LoopbackApiServer.start()` collapses every bind failure to Boolean false; S6 exists but cannot tell denial from port-in-use and gives an ineffective remedy. Required: typed listener-start result (bound / port-in-use / network-permission-denied / other at least); map the cause conservatively, corroborate with permission state where appropriate; denial UI explains and offers app settings; correct manifest/docs/README claims; concise permission inventory. Do NOT ship a runtime requestPermissions(INTERNET) flow merely because the old issue proposed one; a runtime request only after a controlled development-phone proof that this OS build presents a usable request flow and granting behaves correctly; otherwise detection + explanation + app-settings recovery. Any device permission mutation/proof is an owner-controlled device operation: STOP before such a proof if permission state would change.

**#51 — KEEP ISSUE, IMPLEMENT IN THE SAME BRIEF AS #66.** (1) FLAG_SECURE for the Developer API screen's COMPOSITION lifetime (not merely RESUMED), cleared on leaving the screen. (2) The remaining terminal listener failure: terminal accept() IOException while the bound socket was not intentionally closed → close/null the listener, publish terminal state; the screen shows the existing generic S6 rather than pretending the port is healthy. One brief for #66 + #51; both issue numbers kept and closed against their own acceptance evidence.

**#52 — ACCEPT PARTIAL SUPERSESSION / REWRITE-NARROW.** No structured `problems`; no /v1 break. Additive: readable first-problem message/field mappers for asset, event, definition, profile (existing family codes retained); fill `field` for season/condition malformed-value cases where the problem carries it; event bad date/time report `occurredOn` / `occurredTime`; carry `field` through the MCP ApiError and ToolError; correct/document the Errors section. `problems` strings stay wire-compatible; richer structure someday = a new parallel field.

**Order:** #54 → #65 → one combined brief for #66 + #51 → #52. Standing review policy unchanged. For the combined brief, propose the exact denial sentence and Settings-button label before implementation (one ratification batch); #51 reuses S6. For #52, a compact string table of changed/new API contract sentences during planning; no separate product-UX review.

**Report after reconciling:** 1. exact issue title/body changes; 2. the Phase 1A brief structure and dependencies; 3. owner decisions still required; 4. the recommended first brief. STOP there; no implementation until the reconciled plan is approved.
